const { getRobotServers } = require("../models/robotServer");
const { getMember, createMember } = require("../models/serverMembers");
/*
Tool for ensuring all server owners contain the server owner role
*/

const addOwnerRole = async () => {};
